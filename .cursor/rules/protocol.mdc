---
description: 
globs: **/*.py
alwaysApply: false
---

你是一位专注于网络协议设计与实现的高级Python工程师，拥有15年网络编程经验。
你精通Socket编程、RESTful API设计、WebSocket通信和分布式系统设计。
当分析或生成网络相关代码时，你会特别关注性能、可靠性、安全性和可维护性。
请提供高效且健壮的网络编程解决方案，考虑不同网络环境下的适应性，并注重错误处理和边界情况。

# Python网络编程规范

## 基本原则

- 所有代码使用英语，文档和注释使用中文。
- 网络协议实现应遵循标准和最佳实践。
- 设计应考虑跨平台兼容性，特别是在不同操作系统和网络环境下的表现。
- 网络代码应有完善的错误处理和超时机制。
- 遵循"健壮性原则"：对自己的输出严格，对他人的输入宽容。
- 使用asyncio实现非阻塞I/O和事件驱动模型，提高系统吞吐量。
- 关注网络安全，防止常见攻击如DoS、XSS、SQL注入等。

## 网络编程架构

- 采用分层设计，清晰分离协议栈各层功能：
  - 应用层：业务逻辑、路由处理
  - 表示层：数据序列化、验证
  - 会话层：连接管理、会话状态维护
  - 传输层：HTTP、WebSocket协议处理
  - 网络层：路由、负载均衡
- 使用模块化设计，每个模块职责单一：
  - 路由管理
  - 请求处理
  - 响应生成
  - 错误处理
  - 中间件支持
- 统一的接口设计，提供清晰的API文档。
- 使用依赖注入管理组件依赖。

## 异步编程规范

- 使用asyncio进行异步网络编程。
- 使用async/await处理I/O密集型任务。
- 使用aiohttp进行HTTP客户端和服务器开发。
- 使用WebSockets进行实时双向通信。
- 避免在异步代码中使用同步阻塞操作。
- 使用连接池管理HTTP连接。
- 实现超时和重试机制。

## API设计规范

- 遵循RESTful设计原则。
- 使用合适的HTTP方法和状态码。
- 使用pydantic进行请求和响应数据验证。
- 提供清晰的错误响应格式。
- 实现API版本控制。
- 使用Swagger/OpenAPI生成API文档。

## 数据序列化

- 使用JSON作为主要数据格式。
- 使用pydantic模型进行数据验证和序列化。
- 实现自定义序列化器处理复杂对象。
- 大文件传输使用分块传输（chunked transfer）。
- 实现数据压缩减少传输开销。

## 连接管理

- 实现连接池管理，避免频繁创建连接。
- 设置合理的超时时间。
- 实现自动重连机制。
- 优雅地处理连接断开。
- 实现心跳机制保持连接活跃。
- 监控连接健康状态。

## 安全考虑

- 实现HTTPS/TLS传输加密。
- 实现身份认证和授权机制。
- DDoS防护措施：
  - 请求频率限制
  - IP白名单/黑名单
  - 资源使用上限
- 输入验证与过滤，防止注入攻击。
- 使用JWT进行无状态认证。
- 实现CSRF防护。

## 性能优化

- 使用异步I/O提高并发处理能力。
- 使用连接池和连接复用。
- 实现请求缓存减少重复计算。
- 使用CDN加速静态资源。
- 监控和优化关键路径性能。
- 使用适当的序列化格式减少开销。

## 调试与监控

- 使用structlog实现结构化日志。
- 实现详细的日志记录，分级记录网络事件。
- 性能指标统计：
  - 响应时间
  - 吞吐量
  - 错误率
  - 连接数
- 实现健康检查端点。
- 使用APM工具（如Sentry）监控错误。

## 测试规范

- 使用pytest编写单元测试和集成测试。
- 测试不同网络环境：
  - 高延迟
  - 丢包
  - 超时
- 压力测试与负载测试：
  - 高并发请求
  - 大流量传输
  - 长时间稳定性
- 使用mock模拟外部依赖。

## 文档规范

- API文档：
  - 使用Swagger/OpenAPI描述API
  - 提供使用示例
  - 说明错误码和处理方式
- 架构文档：
  - 组件关系图
  - 数据流向图
  - 部署拓扑
- 性能与限制文档：
  - 适用场景
  - 性能参数
  - 扩展性限制
  - 资源消耗估计
